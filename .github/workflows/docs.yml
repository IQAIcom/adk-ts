name: Build and Deploy ADK API Documentation

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'packages/adk/src/**'
      - 'apps/adk-api-docs/**'
      - 'packages/adk/README.md'
      - '.github/workflows/docs.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'packages/adk/src/**'
      - 'apps/adk-api-docs/**'
      - 'packages/adk/README.md'
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build ADK package
        run: pnpm --filter @iqai/adk build

      - name: Install TypeDoc dependencies
        run: pnpm --filter adk-api-docs install

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1'
          bundler-cache: true
          working-directory: apps/adk-api-docs

      - name: Install Ruby dependencies
        run: |
          cd apps/adk-api-docs
          bundle install

      - name: Generate TypeDoc API documentation
        run: |
          cd apps/adk-api-docs
          pnpm run docs:build

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v3

      - name: Build Jekyll site
        run: |
          cd apps/adk-api-docs
          bundle exec jekyll build --baseurl "${{ steps.pages.outputs.base_path }}"
        env:
          JEKYLL_ENV: production

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: apps/adk-api-docs/_site

  # Deployment job
  deploy:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: read
      pages: write
      id-token: write

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4