import { SectionWrapper } from "./section-wrapper";

const CompleteStackSection = () => {
	return (
		<SectionWrapper id="complete-stack" className="bg-white">
			<div>CompleteStackSection</div>
		</SectionWrapper>
	);
};

export default CompleteStackSection;
