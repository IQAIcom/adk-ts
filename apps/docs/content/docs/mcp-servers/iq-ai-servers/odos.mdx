---
title: MCP Odos
description: Find optimal token swap routes and execute trades across DeFi protocols using Odos.
---

import { Callout } from "fumadocs-ui/components/callout";
import { Tab, Tabs } from "fumadocs-ui/components/tabs";

- **Package**: [`@iqai/mcp-odos`](https://www.npmjs.com/package/@iqai/mcp-odos)
- **Provider**: [Odos](https://odos.xyz)

## Overview

The Odos MCP server gives your agent access to Odos's smart order routing for DeFi swaps. It can quote multi-path token swaps across dozens of protocols, assemble optimized swap transactions, and submit them on-chain — all across multiple EVM-compatible networks.

<Callout type="warn" title="Private Key Security">
  This server signs and submits swap transactions using your wallet private key.
  Never commit your private key to source control. Always load it from a secure
  environment variable.
</Callout>

## Getting Started

Install the package:

```bash
pnpm add @iqai/mcp-odos
```

Use the server in your agent:

<Tabs items={["Simple", "Verbose", "Claude Desktop"]}>
  <Tab value="Simple">
    ```typescript
    import { McpOdos } from "@iqai/adk";

    const toolset = McpOdos({
      env: {
        WALLET_PRIVATE_KEY: process.env.WALLET_PRIVATE_KEY,
      },
    });

    const tools = await toolset.getTools();
    ```

  </Tab>

  <Tab value="Verbose">
    ```typescript
    import { McpToolset } from "@iqai/adk";

    const toolset = new McpToolset({
      name: "ODOS MCP Client",
      description: "Client for Odos smart order routing and swaps",
      transport: {
        mode: "stdio",
        command: "npx",
        args: ["-y", "@iqai/mcp-odos"],
        env: {
          WALLET_PRIVATE_KEY: process.env.WALLET_PRIVATE_KEY,
          PATH: process.env.PATH || "",
        },
      },
    });

    const tools = await toolset.getTools();
    ```

  </Tab>

  <Tab value="Claude Desktop">
    ```json
    {
      "mcpServers": {
        "odos-mcp-server": {
          "command": "npx",
          "args": ["-y", "@iqai/mcp-odos"],
          "env": {
            "WALLET_PRIVATE_KEY": "your_wallet_private_key_here"
          }
        }
      }
    }
    ```
  </Tab>
</Tabs>

## Environment Variables

| Variable             | Required | Description                                                         |
| -------------------- | -------- | ------------------------------------------------------------------- |
| `WALLET_PRIVATE_KEY` | Yes      | Private key of the wallet used to sign and submit swap transactions |

## Available Tools

<McpToolsList serverId="odos" />

## Integration Example

```typescript
import { AgentBuilder, McpOdos } from "@iqai/adk";
import * as dotenv from "dotenv";

dotenv.config();

async function main() {
  // Initialize McpOdos toolset
  const toolset = McpOdos({
    env: {
      WALLET_PRIVATE_KEY: process.env.WALLET_PRIVATE_KEY,
    },
  });

  // Get available McpOdos tools
  const odosTools = await toolset.getTools();

  // Create agent with McpOdos tools
  const { runner } = await AgentBuilder.create("odos_agent")
    .withModel("gemini-2.5-flash")
    .withDescription(
      "A DeFi agent that finds and executes optimal token swaps via Odos",
    )
    .withTools(...odosTools)
    .build();

  const response = await runner.ask(
    "Find the best route to swap 1 ETH for USDC on Ethereum and show me the expected output",
  );

  console.log(response);
}

main().catch(console.error);
```

## Further Resources

- [`@iqai/mcp-odos` on npm](https://www.npmjs.com/package/@iqai/mcp-odos)
- [Odos API Docs](https://docs.odos.xyz/)
- [Odos](https://odos.xyz)
