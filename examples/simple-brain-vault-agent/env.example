# Simple Brain Vault Agent Configuration
# Copy this file to .env and fill in your values

# Required: Your wallet private key (keep this secure!)
# Used by both Fraxlend and Odos MCP servers for transaction signing
WALLET_PRIVATE_KEY=0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef

# Optional: Odos API key for better swap routing (not required but recommended)
# ODOS_API_KEY=your-odos-api-key-here

# Optional: LLM Configuration (defaults to gemini-2.0-flash)
# LLM_MODEL=gemini-2.0-flash

# Optional: Debug mode (defaults to true)
# DEBUG=true